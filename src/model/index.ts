export type ArtItem = {
    id: number;
    title: string;
    artist_title: string;
    image_id: string;
    tileIndex: number;
};

export type FavoriteItem = {
    id: number;
    title: string;
    artist_title: string;
    image_id: string;
};
